<%= form_with(model: card, local: true) do |form| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :card_number %>
  </div>

  <div class="field">
    <%= form.label :cvv %>
    <%= form.text_field :cvv, id: :card_cvv %>
  </div>

  <div class="field">
    <%= form.label :expiration %>
    <%= form.text_field :expiration, id: :card_expiration %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :card_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
